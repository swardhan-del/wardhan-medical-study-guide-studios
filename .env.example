# Optional: final public origin. Leave unset for Vercel's generated domain.
# NEXT_PUBLIC_SITE_URL=https://your-domain.example

# Public creator identity and contact email are maintained in src/content/studio.ts.

# Do NOT set LOCAL_CURATION_REVIEW on Vercel. npm run review sets it locally.

# Optional: Search Console HTML-tag verification token (URL-prefix property).
# GOOGLE_SITE_VERIFICATION=

# Optional server-only membership verification contract; no provider is connected yet.
# Leave unset to deny all protected pilot content. Never use NEXT_PUBLIC_ prefixes.
# HTTPS origin only, without a path; POST /v1/session/verify contract in docs/subscription/FOUNDATION.md.
# MEMBERSHIP_VERIFIER_ORIGIN=
# MEMBERSHIP_VERIFIER_API_KEY=
